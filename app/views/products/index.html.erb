<h1>All Products</h1>
  <ul class="products">
    <% @products.each do |product| %>
    <li>
      <%= link_to product.title, product %>
      <%= product.description %>
      Php <%= product.price %>
    </li>
  <% end %>
  </ul>
